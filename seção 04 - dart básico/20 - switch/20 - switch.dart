void main() {
    // switch também é usado para tomar decisões
    // usando if
    String jogo = 'red dead 2';

    if(jogo == 'gta online') {
        print('jogo desde 2014');
      }

    if(jogo == 'red dead 2') {
      print('melhor que já joguei');
    }

    if (jogo == 'Lol') {
      print('uma bosta');

    } 

    // usando switch

    switch(jogo) {
      case 'gta online':
      print('jogo desde 2014');
      break;
    }

      switch(jogo) {
      case 'red dead 2':
      print('melhor que já joguei');
      break;
      }

      switch(jogo) {
      case 'Lol':
      print('uma bosta');
      break;
      }
    }
    
